# Sandbox seed (D-12). Real teammate Slack IDs are added at P4 rollout when the bot
# moves to the company org.
#
# These are real Slack user IDs captured from the personal sandbox workspace
# (Plan 01-02 Section E). Per Path A: `dummy-reviewer` reuses the same Slack ID as
# `kai` for now — Phase 1 schema validates regex shape only, and Phase 2 keystone
# testing can run without a second human. Before Phase 2 reviewer-flow tests
# (specifically THRD-03), replace `dummy-reviewer` with a real second account's
# Slack ID and add that GitHub user as a collaborator on the sandbox repos.
#
# Schema (validated by tests/config-schema.test.ts):
#   - top-level `users:` map
#   - each value matches /^U[A-Z0-9]+$/  (D-05, D-14)
users:
  kai: U0B20676JVB
  dummy-reviewer: U0B20676JVB
